# Build artifacts
/target

# Personal agent instructions (not part of the repository)
/AGENTS.md

# Study notes and planning docs (kept local only)
/docs
/lab

# Editor / OS junk
.DS_Store

# Fuzz campaign results (local runs only — tools/fuzz-kernel.sh)
/fuzz-out
